# 设置页迁移实施清单（Layout-Route 规范版）

## 0. 实施状态
> 本区块由自动化维护，用于记录迁移推进过程、验证结果与阻塞点，保证可追溯。

- 当前任务项：`P-001` Model 页面任务
- 状态：`R-001`、`L-001`、`S-001`、`M-001`、`P-001` 已完成；下一阶段为 `P-002`
- 开始时间：2026-03-17 12:58:54 +08:00
- 结束时间：2026-03-17 15:18:21 +08:00
- 涉及文件（按任务清单与本轮实际核查/修复）：`docs/设置页迁移实施清单.md`、`src/features/settings/routes.tsx`、`src/features/settings/pages/ModelSettings.tsx`、`src/features/settings/api/model.ts`、`src/features/settings/pages/model/page.tsx`、`src/features/settings/pages/model/types.ts`、`src/features/settings/pages/model/hooks/useModelProviders.ts`、`src/features/settings/pages/model/components/ProviderListPane.tsx`、`src/features/settings/pages/model/components/ProviderDetailPane.tsx`、`src/features/settings/pages/model/components/ProviderEditorModal.tsx`、`src/features/settings/pages/model/components/ModelEditorModal.tsx`、`src/styles/app-shell.css`、`e2e-tests/webdriverio/wdio.conf.mjs`、`e2e-tests/webdriverio/scripts/run-wdio.mjs`、`e2e-tests/webdriverio/package.json`、`e2e-tests/webdriverio/tests/helpers/settings-flow.mjs`、`e2e-tests/webdriverio/tests/smoke.desktop.e2e.mjs`、`e2e-tests/webdriverio/tests/smoke.responsive.e2e.mjs`、`package.json`、`src-tauri/src/commands/model.rs`、`src-tauri/src/commands/mod.rs`、`src-tauri/src/models/model.rs`、`src-tauri/src/models/mod.rs`、`src-tauri/src/lib.rs`、`src/bindings.ts`
- 运行前检查结果：
  - 文档与阶段定义：已重新读取本清单中 `P-001` 的定义，并按“改一阶段就跑真实 Tauri WebDriver”执行。
  - 工具可用性：`npm run build`、`npm run e2e:debug`、`npm run e2e:debug:desktop`、`npm run e2e:debug:responsive`、`git status --short` 可执行；当前工作树存在其他文件改动，但与本轮设置页迁移无直接冲突。
  - WebDriver 基线：`e2e-tests/webdriverio` 已拆分为真实桌面 smoke 与响应式 smoke，两者都通过真实 Tauri debug 二进制启动。
- 构建/脚本/行为核查结果：
  - `npm run build`：通过；`P-001` 拆分后的模型页结构可正常编译。
  - `npm run e2e:debug:desktop`：通过；真实 Tauri debug 路径可自动登录、进入 `/settings/gemini`、切换到 `/settings/model`，自动创建平台、添加模型并执行健康检查，全程保持桌面窗口。
  - `npm run e2e:debug:responsive`：通过；真实 Tauri debug 路径从登录开始即保持窄屏窗口，只校验独立移动端 settings topbar/tabs，不再混入桌面断言。
  - `npm run e2e:debug`：通过；聚合入口会同时覆盖 `desktop + responsive` 两条 smoke。
  - 行为核查：
    - `R-001`：`/settings/*` 已从 `MainLayout + Sidebar` 分支剥离。
    - `L-001`：settings 独立 shell、registry、返回入口与页面头部已接入。
    - `S-001`：全局 `Sidebar` 已移除 settings 模式分支，仅保留进入设置的一级入口。
    - `M-001`：移动端 `SettingsTopbar + SettingsMobileTabs` 已启用，当前 tab 会自动滚动到可见区域。
    - `P-001`：模型页已迁入 `pages/model/` 目录，拆分为 `api + hooks + list/detail panes + modals`；前端已切换到 model 领域 API，后端已补齐 `list/create/update/delete provider`、`model upsert/delete`、`protocol/enabled`、`health check` 命令，健康检查不再依赖随机前端 mock。
- 发现的问题与阻塞点：
  - `P-001` 已按任务要求补齐 model 领域命令，但其他设置页仍大量依赖 `getSettings/updateSettings`；后续阶段需要按同一标准逐页替换。
  - Rust/Tauri 编译仍存在大量 warning，但不阻塞当前 WebDriver 自动化链路。
  - 两条独立 Tauri WebDriver 调试命令不能并行启动，否则会争抢 `tauri-driver` 的 `4444` 端口；后续阶段需按串行方式运行 `desktop` 与 `responsive` smoke。
- 下一阶段是否允许开始：是；可进入 `P-002`（Tools 页面任务）。
- 预期输出：在保留当前独立 settings shell、已完成模型页拆分、且 `desktop + responsive` WebDriver 基线持续通过的前提下，继续逐页迁移。

### 0.1 状态总览（按实施顺序）
- `R-001`：完成
- `L-001`：完成
- `S-001`：完成
- `M-001`：完成
- `P-001`：完成
- `P-002`~`P-009`：未开始

---

## 1. 目标与结论
本文件定义 `AionX` 设置页的最终目标架构、页面规范与实施顺序。

唯一结论如下：

1. 设置页是 `AionX` 的一级功能模块，不再视作 `AionUi` modal 内容的 page 化包装。
2. `layout-route` 是唯一允许的承载方案：`AppLayout` 与 `SettingsLayout` 平级，`/settings -> SettingsLayout -> Outlet -> page`。
3. 不再为向后兼容保留 page wrapper、modal/page 双态判断、AionUi 文案、主工作区侧栏复用或旧式泛化设置读写方式。
4. 所有页面、样式、导航、状态和后端接口都围绕 `AionX` 自身产品语义重建。

本方案追求的是规范、清晰、可维护，而不是“尽量少改”。

---

## 2. 非目标
以下内容不属于本次迁移目标，且应在实施中主动避免：

- 不再保留“每个页面内部包一层 `SettingsPageWrapper`”的结构。
- 不再保留 `SettingsViewModeProvider` / `modal | page` 双模式分支。
- 不再把 settings 专用导航挂在全局 `Sidebar` 的二级状态里。
- 不再在 `/settings/*` 下复用主工作区的聊天历史、快捷入口和底部操作区。
- 不再以 `AionUi` 的目录结构、组件命名、文案内容作为兼容前提。
- 不再把 `getSettings/updateSettings` 作为设置页核心业务接口的长期方案。
- 不再使用 `window.prompt`、原始 JSON 文本框、占位按钮等临时交互作为最终实现。

---

## 3. 最终架构决策

### 3.1 路由结构
设置页采用与主工作区平级的独立 shell：

```tsx
RootLayout
|- AppLayout
|  |- /guid
|  |- /conversation/:id
|  |- /cron
|  |- ...
|- SettingsLayout
   |- /settings/gemini
   |- /settings/model
   |- /settings/agent
   |- /settings/display
   |- /settings/webui
   |- /settings/system
   |- /settings/tools
   |- /settings/about
   |- /settings/ext/:tabId
```

约束如下：

- `SettingsLayout` 是独立于 `AppLayout` 的设置工作区壳层。
- `SettingsLayout` 拥有 settings 模块的导航、页面标题、移动端切换、错误态和空态承载职责。
- 各子页面只负责本页面的内容与交互，不再负责导航壳层。
- 扩展 tab 仍使用 `ext/:tabId` 路由，但 tab 注入由 settings 模块内部注册表统一处理。
- `/settings/*` 进入后不再显示主工作区完整侧栏。

### 3.2 导航归属
导航归属做如下重构：

- `AppLayout` 只负责主工作区导航。
- `SettingsLayout` 只负责设置工作区导航。
- 二者不能同时显示两套完整侧栏。

桌面端规则：

- 进入 `/settings/*` 后切换为独立 settings shell。
- 左侧只显示 settings 导航列。
- 左上显示“返回应用”入口。
- 不显示聊天历史、批量操作、新建会话等主工作区元素。

移动端规则：

- 进入 `/settings/*` 后不显示桌面式左侧导航。
- 顶部显示返回应用按钮、当前页面标题、可选搜索/操作按钮。
- 设置分类通过顶部横向 tabs 或分组列表切换。
- 页面内容为单列滚动布局。

返回位置规范：

- 桌面端返回入口位于 settings shell 顶部左上区域，可放在 `SettingsSidebar` 顶部或 `SettingsTopbar` 左侧。
- 移动端返回入口位于 `SettingsTopbar` 左上。
- 底部禁止放“返回应用”或“返回上一层”。
- 页面底部仅允许放保存、取消、重置、测试连接等页面操作。

这是 layout-route 架构下更规范的职责划分：

- 主工作区导航归 `AppLayout`。
- 设置导航归 `SettingsLayout`。
- 页面内容归子路由。

### 3.3 页面模式
设置页被定义为纯页面模块，因此：

- 移除 `settingsViewContext.tsx` 的业务意义。
- 不再为 modal 兼容保留 `page` / `modal` 分支。
- `SettingsPageWrapper.tsx` 不再作为独立包装器存在；其职责并入 `SettingsLayout` 与共享页面容器。
- 进入设置时整体切换工作区，而不是在主工作区内容区中嵌套一个设置页。

### 3.4 Shell 形态
最终 shell 形态参考 Codex 桌面版：

- 桌面端：`SettingsSidebar + SettingsContent`
- 移动端：`SettingsTopbar + SettingsTabs + SettingsContent`

禁止形态：

- `MainSidebar + SettingsSidebar + Content`
- `MainSidebar + MainContent(其中再嵌 SettingsNav + Page)`
- 页面内部自行模拟左侧导航

---

## 4. 目标目录结构
目标目录应从“若干平铺页面文件”演进为“layout + registry + shared + per-page domain”结构：

```text
src/features/settings/
  routes.tsx
  registry/
    settingsRegistry.ts
    extensionRegistry.ts
  layout/
    SettingsLayout.tsx
    SettingsNav.tsx
    SettingsMobileTabs.tsx
    SettingsTopbar.tsx
    SettingsBackLink.tsx
    SettingsHeader.tsx
  shared/
    SettingsPageFrame.tsx
    SettingsSectionCard.tsx
    SettingsSplitView.tsx
    SettingsEmptyState.tsx
    SettingsLoadingState.tsx
    SettingsErrorState.tsx
    PreferenceRow.tsx
  api/
    gemini.ts
    model.ts
    agent.ts
    display.ts
    webui.ts
    system.ts
    tools.ts
    about.ts
    extension.ts
  pages/
    gemini/
      page.tsx
      components/
      hooks/
      types.ts
    model/
      page.tsx
      components/
      hooks/
      types.ts
    agent/
      page.tsx
      components/
      hooks/
      types.ts
    display/
      page.tsx
    webui/
      page.tsx
      components/
      hooks/
      types.ts
    system/
      page.tsx
    tools/
      page.tsx
      components/
      hooks/
      types.ts
    about/
      page.tsx
    extension/
      page.tsx
      ExtensionSettingsHost.tsx
      hooks/
      types.ts
```

实施要求：

- 不再继续沿用 `pages/*.tsx` 平铺文件作为最终结构。
- 每个页面域必须有自己的 `components/` 与 `hooks/` 子目录，避免继续堆大文件。
- `commands` 只允许出现在 `src/features/settings/api/*`，页面组件不直接调用 Tauri 命令。
- `SettingsLayout` 必须是独立工作区壳，不得 import 主工作区聊天历史或主工作区专属组件。

---

## 5. 前端分层规范

### 5.1 Layout 层
`SettingsLayout` 必须承担以下职责：

- 读取 settings 注册表并生成导航。
- 负责桌面端 settings 左侧导航。
- 负责移动端顶部返回栏与顶部 tabs。
- 根据当前路由显示页面标题、副标题、状态提示。
- 承载 `<Outlet />`。
- 统一处理 loading、error、not-found 和 extension tab 缺失等上层状态。
- 统一处理“返回应用”的导航动作。

`SettingsLayout` 不负责：

- 页面内表单字段逻辑。
- 页面内业务数据写回。
- 页面内弹窗状态。

### 5.2 Registry 层
新增 `settingsRegistry.ts`，作为 settings 模块唯一导航与页面元信息来源。

每个 tab 至少包含：

- `id`
- `path`
- `label`
- `icon`
- `title`
- `description`
- `widthPreset`
- `layoutMode`
- `order`
- `mobileVisible`

推荐结构：

```ts
type SettingsWidthPreset = 'narrow' | 'regular' | 'wide' | 'full';
type SettingsLayoutMode = 'form-stack' | 'split-view' | 'tabbed' | 'host';
```

扩展 tab 注入规则：

- 内置 tab 先注册。
- 扩展 tab 通过 `extensionRegistry.ts` 读取后合并。
- 合并逻辑由 registry 负责，不允许散落在 `Sidebar`、页面组件或样式文件中。

### 5.3 数据加载层
前端数据流采用“路由加载 + 页面局部交互状态”的方式：

- 路由进入时通过 page loader 或页面初始化 hook 拉取快照。
- 页面持久化数据以服务端快照为准。
- 组件 state 只保存草稿、展开态、选中态、弹窗态等临时状态。
- 修改完成后通过领域 API 提交，再局部更新或重新拉取。

禁止事项：

- 页面组件直接 `commands.xxx()`。
- 一个页面既负责请求、又负责数据变换、又负责大段展示。
- 把 localStorage 当作设置真源。

### 5.4 共享组件层
共享组件只提供结构与视觉，不承担领域逻辑。

必须保留或新增的共享组件：

- `SettingsPageFrame`
- `SettingsSectionCard`
- `SettingsSplitView`
- `SettingsEmptyState`
- `SettingsLoadingState`
- `SettingsErrorState`
- `PreferenceRow`

应移除或重命名的旧组件：

- `SettingsPage.tsx` 作为旧式 section/card 渲染器，不再作为最终页面基类。
- `settingsConfig.ts` 迁移为 registry 后不再保留旧定义。

---

## 6. 后端接口规范

### 6.1 总体原则
后端接口按领域拆分，不再以 `get_settings(category)` / `update_settings(category, settings)` 作为主业务协议。

最终形态要求：

- 每个页面至少有一组专属命令。
- 命令返回 typed 结构，而不是页面自行拼装 `JsonValue`。
- 后端完成校验、默认值填充和持久化。
- 前端不承担复杂 schema 修复逻辑。

### 6.2 泛化设置接口的定位
现有 `get_settings` / `update_settings` 只允许保留为：

- 迁移期兜底。
- 低价值、全局性、小体量配置项。

以下页面禁止继续把泛化设置接口作为最终主通道：

- Gemini
- Model
- Agent
- Tools
- WebUI
- Extension

### 6.3 建议的领域命令
建议新增或收口为以下命令族：

- Gemini: `get_gemini_settings`, `save_gemini_settings`, `get_google_auth_status`, `start_google_auth`, `logout_google_auth`
- Model: `list_model_providers`, `create_model_provider`, `update_model_provider`, `delete_model_provider`, `run_model_health_check`
- Agent: `list_builtin_assistants`, `list_custom_assistants`, `create_custom_assistant`, `update_custom_assistant`, `delete_custom_assistant`, `save_builtin_assistant_preferences`
- Display: `get_display_settings`, `save_display_settings`, `set_zoom_factor`, `save_custom_css`
- WebUI: `get_webui_settings`, `get_webui_status`, `start_webui`, `stop_webui`, `change_webui_password`, `reset_webui_password`
- Channel: `list_channel_plugins`, `create_channel_plugin`, `update_channel_plugin`, `delete_channel_plugin`, `validate_channel_plugin_config`
- System: `get_system_settings`, `save_system_settings`, `get_system_info`, `get_system_directories`, `update_runtime_directories`
- Tools: `list_mcp_servers`, `create_mcp_server`, `update_mcp_server`, `delete_mcp_server`, `test_mcp_connection`, `get_image_generation_settings`, `save_image_generation_settings`
- About: `get_app_metadata`, `check_for_updates`, `get_update_preferences`, `save_update_preferences`
- Extension: `list_extension_settings_tabs`, `get_extension_settings_tab`, `set_extension_enabled`, `get_extension_host_context`

---

## 7. 样式与页面布局规范

### 7.1 统一样式骨架
settings 模块必须采用统一壳层类名，不再依赖零散的旧类：

- `.settings-layout`
- `.settings-layout__sidebar`
- `.settings-layout__main`
- `.settings-layout__topbar`
- `.settings-layout__header`
- `.settings-layout__body`
- `.settings-layout__content`
- `.settings-mobile-tabs`
- `.settings-mobile-tabs__item`
- `.settings-back-link`
- `.settings-page`
- `.settings-page--narrow`
- `.settings-page--regular`
- `.settings-page--wide`
- `.settings-page--full`
- `.settings-section-card`
- `.settings-split-view`
- `.settings-split-view__list`
- `.settings-split-view__detail`

旧类如 `.settings-page-shell`、`.settings-card`、`.settings-card__body` 可在迁移期复用，但最终应收敛到新的模块命名。

### 7.2 宽度规范
页面宽度统一由 registry 决定，不允许每页自行写死：

- `narrow`: 520px，适用于 About
- `regular`: 760px，适用于 Gemini / Display / System
- `wide`: 1040px，适用于 Model / Agent / Tools / WebUI
- `full`: 1280px，适用于 Extension host

### 7.3 页面模板
settings 页面只允许使用以下四种模板：

- `form-stack`
  适用于表单与配置卡片纵向堆叠页。
- `split-view`
  适用于列表 + 详情的主从页。
- `tabbed`
  适用于单路由内部仍有二级分组的页。
- `host`
  适用于嵌入外部 UI 宿主页。

### 7.4 移动端规范
移动端必须满足：

- 进入设置后切换为独立移动端 settings shell。
- 顶部必须保留返回应用入口。
- 保留模块顶部横向 tabs 或等价分类切换器。
- 页面内容与桌面侧栏解耦，不依赖全局 `Sidebar` 的展开状态。
- 列表/详情类页面在移动端退化为单列堆叠、抽屉或独立详情页，不允许挤压成桌面双栏。
- 所有按钮尺寸、点击区域和间距满足手指操作场景。
- 顶部返回、标题、tabs、页面内容之间保持稳定层级，不允许多个 sticky 区域互相挤压。

---

## 8. 各页面实施细则

### 8.1 Gemini 页面
目标定位：账号、认证、网络与默认行为配置页。

页面模板：

- 宽度：`regular`
- 模板：`form-stack`

页面内容：

- 账号状态卡：Google 登录状态、当前账号邮箱、登录/退出按钮
- 项目绑定卡：当前账号对应的 `GOOGLE_CLOUD_PROJECT`
- 网络配置卡：代理、基础 URL、认证方式
- 默认行为卡：偏好模式、Yolo 模式等

前端实现要求：

- 不允许再使用 `window.prompt` 录入邮箱。
- 页面以 loader 或初始化 hook 获取完整 `GeminiSettingsDto`。
- 表单草稿与服务端快照分离。
- 文本字段使用显式保存或短延迟自动保存，但账号登录状态必须走独立命令。

后端接口要求：

- `get_gemini_settings`
- `save_gemini_settings`
- `get_google_auth_status`
- `start_google_auth`
- `logout_google_auth`

样式要求：

- 卡片纵向堆叠。
- 登录状态区域使用首屏 hero card。
- 项目绑定与代理字段不可混在同一行压缩展示。

完成标准：

- 页面进入即显示真实账号状态。
- 支持账号切换后的 project 绑定。
- 所有持久化字段不再依赖前端自行拼装 `JsonValue`。

### 8.2 Model 页面
目标定位：模型平台、模型列表、协议、健康检查与默认策略管理页。

页面模板：

- 宽度：`wide`
- 模板：`split-view`

页面内容：

- 左栏：Provider 列表、创建按钮、筛选与搜索
- 右栏：Provider 详情、模型列表、协议 badge、健康状态、启用状态
- 辅助弹层：新增 Provider、编辑 Provider、新增模型

前端实现要求：

- 页面拆成 `ProviderListPane`、`ProviderDetailPane`、`ProviderEditorModal`、`ModelEditorModal`。
- 健康检查为独立 mutation，不允许继续写本地 mock latency。
- 模型协议切换、批量启停、API Key 数量统计由 detail pane 负责。

后端接口要求：

- `list_model_providers`
- `create_model_provider`
- `update_model_provider`
- `delete_model_provider`
- `run_model_health_check`

样式要求：

- 桌面端采用主从双栏。
- 左栏固定宽度 280~320px。
- 右栏详情支持内部 section card。
- 移动端退化为“列表页 -> 详情抽屉/单列页”。

完成标准：

- Provider CRUD、模型 CRUD、协议切换、健康检查都是真实链路。
- 页面不再直接读写 `model.config` 原始结构。

### 8.3 Agent 页面
目标定位：内置助手、自定义助手与 Agent 绑定策略管理页。

页面模板：

- 宽度：`wide`
- 模板：`split-view`

页面内容：

- 左栏：builtin/custom 分组列表、创建按钮、状态标签
- 右栏：助手详情、Prompt、主 Agent、启停状态、编辑入口
- 辅助弹层：创建/编辑自定义助手

前端实现要求：

- 内置助手与自定义助手展示模型统一。
- builtin 配置与 custom plugin 配置分开保存，但页面统一表现。
- 页面不继续堆一个超长文件；拆分 list、detail、editor、manifest adapter。

后端接口要求：

- `list_builtin_assistants`
- `list_custom_assistants`
- `create_custom_assistant`
- `update_custom_assistant`
- `delete_custom_assistant`
- `save_builtin_assistant_preferences`
- `detect_agents`

样式要求：

- 主从双栏。
- 右栏详情含 hero 区、说明区、Prompt 代码块区。
- builtin/custom 标签视觉统一，不用 emoji 作为唯一识别手段。
- 移动端退化为上下结构，详情区位于列表下方或进入单独详情页。

完成标准：

- 内置与自定义助手都走统一列表。
- 主 Agent 绑定来自真实 `detect_agents` 结果。

### 8.4 Display 页面
目标定位：主题、缩放、窗口表现和自定义样式配置页。

页面模板：

- 宽度：`regular`
- 模板：`form-stack`

页面内容：

- 外观卡：主题模式、系统跟随、标题栏模式
- 缩放卡：缩放比例、预览说明
- 高级卡：自定义 CSS、重置操作

前端实现要求：

- `customCss` 不再配“与 AionUi 一样”的文案。
- 缩放操作调用独立命令，设置存储与即时效果分离。
- 自定义 CSS 提供保存、恢复默认和即时预览控制。

后端接口要求：

- `get_display_settings`
- `save_display_settings`
- `set_zoom_factor`
- `save_custom_css`

样式要求：

- 使用常规卡片堆叠。
- 自定义 CSS 编辑器占满卡片宽度。
- 页面文案全部以 AionX 为主。

完成标准：

- 显示设置从 UI 到持久化完全自洽。
- 不再出现 AionUi 对齐文案。

### 8.5 WebUI 页面
目标定位：桌面 WebUI 服务、密码与通道插件管理页。

页面模板：

- 宽度：`wide`
- 模板：`tabbed`

页面内容：

- 子 tab 1：服务
  - 启停状态
  - 地址与端口
  - 远程访问
  - 管理员用户名/初始密码
  - 修改密码、重置密码
- 子 tab 2：频道
  - Channel plugin 列表
  - 各频道配置表单
  - 启停与创建/编辑

前端实现要求：

- `WebUI` 与 `Channels` 拆成两个子组件，不继续混在单页大文件。
- 不再以 JSON 文本作为主要 channel 配置 UI，JSON 仅保留高级模式。
- 状态刷新与命令执行分开。

后端接口要求：

- `get_webui_settings`
- `get_webui_status`
- `start_webui`
- `stop_webui`
- `change_webui_password`
- `reset_webui_password`
- `list_channel_plugins`
- `create_channel_plugin`
- `update_channel_plugin`
- `delete_channel_plugin`
- `validate_channel_plugin_config`

样式要求：

- 页面顶部使用子 tab，不再混用全局路由层 tabs。
- 服务卡和频道列表卡分区清晰。
- 密码管理区与运行状态区分开。
- 移动端下频道列表不与服务卡并列，采用顺序堆叠。

完成标准：

- 启停、密码、频道配置均为真实能力。
- 频道表单不再以统一 JSON 文本框作为主交互。

### 8.6 System 页面
目标定位：语言、托盘、目录、系统信息与运行时配置页。

页面模板：

- 宽度：`regular`
- 模板：`form-stack`

页面内容：

- 应用行为卡：语言、关闭到托盘、启动行为
- 目录卡：缓存目录、工作目录、日志目录
- 诊断卡：系统信息、运行时版本、故障排查入口

前端实现要求：

- 目录修改要有确认流程和保存后反馈。
- 语言切换属于全局副作用，需要同步 i18n 与后端设置。
- 页面不直接自己拼 `runtimeInfo` 兜底逻辑；应由后端返回规范结构。

后端接口要求：

- `get_system_settings`
- `save_system_settings`
- `get_system_info`
- `get_system_directories`
- `update_runtime_directories`

样式要求：

- 常规卡片堆叠。
- 路径输入区使用“只读输入 + 操作按钮”规范。

完成标准：

- 系统页所有字段都有明确后端来源。
- 不再由页面自己同时读多个泛化 settings key 拼模型。

### 8.7 Tools 页面
目标定位：MCP 工具生态与图像生成能力管理页。

页面模板：

- 宽度：`wide`
- 模板：`split-view`

页面内容：

- 左栏：MCP server 列表、状态、筛选、创建入口
- 右栏：server 详情、连接测试、OAuth、环境变量、参数配置
- 辅助区域：图像生成设置

前端实现要求：

- MCP 管理拆成 list、detail、editor、test action、oauth action。
- `ToolsSettings.tsx` 不再承载全部逻辑。
- 图像生成配置独立成小卡片区，但仍属于 Tools 页面。

后端接口要求：

- `list_mcp_servers`
- `create_mcp_server`
- `update_mcp_server`
- `delete_mcp_server`
- `test_mcp_connection`
- `get_image_generation_settings`
- `save_image_generation_settings`

样式要求：

- 主从布局。
- 状态 badge、测试按钮、OAuth 状态放在详情栏头部。
- 图像生成配置作为独立 section，不插进 server 列表。
- 移动端以 server 列表优先，详情通过抽屉或独立页打开。

完成标准：

- MCP 为真实管理页，而不是若干按钮堆叠。
- 工具页结构足以继续扩展 OAuth、批量导入、只读扩展 server 等能力。

### 8.8 About 页面
目标定位：品牌、版本、更新与外链信息页。

页面模板：

- 宽度：`narrow`
- 模板：`form-stack`

页面内容：

- Hero 卡：产品名、版本、仓库链接
- 更新卡：检查更新、是否包含预发布
- 资源卡：文档、更新日志、问题反馈、官网

前端实现要求：

- 删除全部“保持与 AionUi 一致”的描述。
- 资源链接全部切换到 AionX 自身地址。
- 更新按钮必须绑定真实逻辑；若暂未实现，则此功能不得进入完成态。

后端接口要求：

- `get_app_metadata`
- `check_for_updates`
- `get_update_preferences`
- `save_update_preferences`

样式要求：

- 采用窄栏居中。
- Hero 信息置顶，外链列表在下。

完成标准：

- 关于页完全去 AionUi 化。
- 更新操作不再是空按钮。

### 8.9 Extension 页面
目标定位：扩展设置宿主页与降级信息页。

页面模板：

- 宽度：`full`
- 模板：`host`

页面内容：

- 若存在扩展 settings host：
  - 页面头部显示扩展名、版本、来源
  - 主体渲染 `ExtensionSettingsHost`
- 若不存在 host：
  - 渲染 metadata fallback，包括名称、版本、路径、描述、启用状态

前端实现要求：

- 页面按 `tabId` 读取扩展 settings tab 声明，不再只在扩展列表里按 `id/name` 猜。
- `iframe` / `webview` 承载逻辑抽到 `ExtensionSettingsHost.tsx`。
- locale、theme、snapshot 等桥接上下文统一由 host 注入。

后端接口要求：

- `list_extension_settings_tabs`
- `get_extension_settings_tab`
- `set_extension_enabled`
- `get_extension_host_context`

样式要求：

- 作为 `full` 宽度页，不受常规表单宽度限制。
- host 区域独立边框与最小高度。
- fallback metadata 页采用常规卡片样式。

完成标准：

- 扩展可向 settings 注入真实页面。
- 元信息页只作为兜底，而不是默认实现。

---

## 9. 页面一览表
为避免页面目标漂移，统一使用下表作为快速索引：

| 页面 | 路由 | 宽度 | 模板 | 前端主状态 | 最终后端接口形态 |
| --- | --- | --- | --- | --- | --- |
| Gemini | `/settings/gemini` | regular | form-stack | 表单草稿 + 账号状态 | typed Gemini commands |
| Model | `/settings/model` | wide | split-view | 选中 provider + CRUD | typed model commands |
| Agent | `/settings/agent` | wide | split-view | 选中 assistant + editor | typed assistant commands |
| Display | `/settings/display` | regular | form-stack | 主题/缩放草稿 | typed display commands |
| WebUI | `/settings/webui` | wide | tabbed | 服务状态 + channel editor | typed webui/channel commands |
| System | `/settings/system` | regular | form-stack | 应用行为 + 目录确认 | typed system commands |
| Tools | `/settings/tools` | wide | split-view | server list/detail + image config | typed mcp/tools commands |
| About | `/settings/about` | narrow | form-stack | 更新偏好 | typed about/update commands |
| Extension | `/settings/ext/:tabId` | full | host | host context + fallback state | typed extension commands |

---

## 10. 代码级清理要求
以下文件或职责需要明确清理或重构：

- `src/components/layout/Sidebar.tsx`
  - 删除 settings 子导航分支。
  - 只保留“进入设置”一级入口。
  - `/settings/*` 下不再作为主内容壳层出现。
- `src/features/settings/components/settingsConfig.ts`
  - 替换为 registry。
- `src/features/settings/components/settingsViewContext.tsx`
  - 删除。
- `src/features/settings/components/SettingsPage.tsx`
  - 重构为 `SettingsPageFrame` 或移除。
- `src/features/settings/pages/*.tsx`
  - 迁入按页面域分目录结构。
- `src/styles/app-shell.css`
  - 收敛 settings 类名，去掉过时命名。

---

## 11. 实施顺序

### 阶段 0：断舍离
- 删除 page wrapper 兼容思路。
- 删除 settings 在全局 `Sidebar` 的二级导航模式。
- 确认 `/settings/*` 从 `AppLayout` 中剥离为独立 shell。
- 标记 `settingsViewContext`、旧 `settingsConfig`、旧 `SettingsPage` 为待移除。

### 阶段 1：搭建 layout-route 基础骨架
- 新建 `registry/`、`layout/`、`shared/` 目录。
- 重写 `SettingsLayout`。
- 引入 settings 专属左侧导航、返回入口与移动端 topbar/tabs。
- 将全局 `Sidebar` 收敛为主工作区导航，不再参与 settings 壳层。

### 阶段 2：建立 typed API 层
- 为每个页面新增 `api/*.ts`。
- 将页面中的直接 `commands` 调用迁出到 API 层。
- 规划 Rust command 收口路径。

### 阶段 3：逐页迁移
推荐顺序：

1. Model
2. Tools
3. Gemini
4. WebUI
5. Extension
6. Agent
7. System
8. Display
9. About

理由：

- 前五页决定 settings 模块的结构能力、生态能力和后端边界是否成立。
- 后四页相对简单，适合在骨架稳定后快速收尾。

### 阶段 4：统一样式与文案
- 收敛类名。
- 删除 AionUi 相关文案。
- 调整宽度、间距、移动端行为与空态/错误态。

### 阶段 5：移除旧实现
- 删除旧式 wrapper 思路残留。
- 删除不再使用的共享组件和兼容分支。
- 停止新增基于 `getSettings/updateSettings` 的页面逻辑。

---

## 12. 验收标准
迁移完成必须同时满足以下条件：

### 12.1 架构验收
- `/settings` 下所有页面都由 `SettingsLayout` 承载。
- `/settings/*` 不再复用 `AppLayout` 主工作区壳层。
- settings 导航不再位于全局 `Sidebar`。
- 所有页面都在 registry 中注册。
- 不存在 `SettingsViewModeProvider` 与 `modal|page` 分支。
- 桌面端不存在双侧栏并列。
- 移动端存在独立的 settings topbar 与分类切换。

### 12.2 页面验收
- 每页都符合既定宽度与模板。
- 每页都有明确的 title、description、section 划分。
- 页面级 loading、error、empty state 统一。

### 12.3 前端验收
- settings 页面组件不直接 import `commands`。
- 大页面已经拆分为 page + components + hooks + api。
- 本地 state 只保存临时 UI 状态，不保存最终业务真值。

### 12.4 后端验收
- Gemini / Model / Agent / Tools / WebUI / Extension 已具备领域接口。
- 重要页面不再依赖泛化 `getSettings/updateSettings` 作为最终协议。
- 更新、认证、健康检查、扩展 host 均为真实能力。

### 12.5 文案验收
- 不再出现“与 AionUi 一致”之类描述。
- 所有资源链接与品牌信息归属 AionX。

---

## 13. 最终判断标准
如果某个实现满足以下任一情况，则视为不符合本规范：

- 页面内部仍自行包 wrapper。
- settings 导航仍挂在全局 `Sidebar` 的 settings 模式分支。
- `/settings/*` 仍显示主工作区聊天历史或主工作区侧栏完整内容。
- 桌面端出现两套同权重侧栏并列。
- 页面组件直接写 `commands.xxx()`。
- 页面核心能力仍依赖 mock、prompt、空按钮或 JSON 主交互。
- 文案仍以 AionUi 为参照对象。

本次迁移完成的标志，不是“页面看起来差不多”，而是：

> `AionX` 设置页成为一个由 `layout-route` 驱动、边界清晰、页面规范统一、后端接口按领域收口的独立模块。

---

## 14. 开发任务拆解
本节将目标架构拆为可直接排期和编码的任务。所有任务默认按先基础壳层、再导航、再页面的顺序执行。

### 14.1 路由重构任务

#### 任务 ID
`R-001`

#### 目标
将 `/settings/*` 从 `AppLayout + Sidebar` 体系中剥离，切换为与主工作区平级的 `SettingsLayout` 路由分支。

#### 涉及文件
- `F:\Work\AionX\src\router.tsx`
- `F:\Work\AionX\src\features\settings\routes.tsx`
- `F:\Work\AionX\src\features\auth\components\ProtectedLayout.tsx`
- 新增：`F:\Work\AionX\src\features\auth\components\ProtectedOutlet.tsx` 或等价受保护路由壳

#### 实施步骤
1. 将当前 `router.tsx` 中的受保护根路由拆成“受保护根 + 子分支”结构。
2. 保留主工作区分支：`MainLayout sider={<Sidebar />}`，仅承载 `/guid`、`/conversation/:id`、`/cron`、`/test/components` 等非 settings 路由。
3. 新增 settings 分支：`SettingsLayout` 直接承载 `/settings/*`。
4. 将 `settingsRoutes` 迁移为 settings 分支 children，而不是继续作为 `MainLayout` 的 children。
5. 保留 `/settings` 到默认页的重定向，例如 `/settings/gemini`。
6. 验证登录态失效时，主工作区与 settings 分支都能正确跳回 `/login`。

#### 交付结果
- `router.tsx` 中存在两个受保护子分支：`AppLayout` 分支和 `SettingsLayout` 分支。
- `/settings/*` 已不再由 `MainLayout` 承载。

#### 验收标准
- 进入 `/settings/gemini` 时，页面不显示主工作区侧栏。
- 主工作区页面行为不受 settings 分支改造影响。
- 浏览器前进后退、登录跳转、默认重定向都正常。

#### 前置依赖
- 无。

---

### 14.2 SettingsLayout 骨架任务

#### 任务 ID
`L-001`

#### 目标
搭建 settings 独立 shell，包括桌面端 settings 导航、顶部返回入口、页面头部、移动端壳层与内容区。

#### 涉及文件
- 新增：`F:\Work\AionX\src\features\settings\layout\SettingsLayout.tsx`
- 新增：`F:\Work\AionX\src\features\settings\layout\SettingsNav.tsx`
- 新增：`F:\Work\AionX\src\features\settings\layout\SettingsTopbar.tsx`
- 新增：`F:\Work\AionX\src\features\settings\layout\SettingsBackLink.tsx`
- 新增：`F:\Work\AionX\src\features\settings\layout\SettingsHeader.tsx`
- 新增：`F:\Work\AionX\src\features\settings\registry\settingsRegistry.ts`
- `F:\Work\AionX\src\features\settings\components\SettingsLayout.tsx`（迁移或删除旧实现）
- `F:\Work\AionX\src\styles\app-shell.css`

#### 实施步骤
1. 建立 `settingsRegistry.ts`，定义内置 tab 的 `id/path/label/title/description/icon/widthPreset/layoutMode/order/mobileVisible`。
2. 用新的 `SettingsLayout` 替代旧的最小容器实现。
3. 在桌面端渲染 `SettingsNav + SettingsHeader + Outlet`。
4. 在移动端渲染 `SettingsTopbar + SettingsMobileTabs + Outlet`。
5. 在 layout 层统一处理当前页 title、description、not-found、扩展 tab 缺失态。
6. 明确内容滚动由 `settings-layout__content` 承载，不让子页面重复控制外层滚动。

#### 交付结果
- settings 独立 shell 可用。
- 内置页面全部可通过 registry 渲染导航。
- 页面顶部信息由 layout 层统一显示。

#### 验收标准
- 桌面端存在 settings 专属左侧导航。
- 移动端存在顶部返回栏和分类切换。
- 返回入口固定在顶部左上，底部没有返回按钮。

#### 前置依赖
- `R-001`。

---

### 14.3 Sidebar 收敛任务

#### 任务 ID
`S-001`

#### 目标
将全局 `Sidebar` 收敛为主工作区导航，删除 settings 模式分支与 settings 子导航逻辑。

#### 涉及文件
- `F:\Work\AionX\src\components\layout\Sidebar.tsx`
- `F:\Work\AionX\src\components\layout\MainLayout.tsx`
- `F:\Work\AionX\src\features\settings\components\settingsConfig.ts`
- `F:\Work\AionX\src\styles\app-shell.css`

#### 实施步骤
1. 删除 `Sidebar.tsx` 中 `isSettings` 分支、`settingsLinks` 静态数组和返回聊天逻辑。
2. 保留一个一级“设置”入口，仅负责跳转到 `/settings/gemini`。
3. 删除 settings 模式下的主题切换、settings item 高亮、settings root sider 样式依赖。
4. 如果 `MainLayout` 中对 settings 模式有额外点击/遮罩逻辑，删除相关条件分支。
5. 将旧 `settingsConfig.ts` 标记为待移除，后续统一改由 registry 驱动。

#### 交付结果
- `Sidebar` 只服务主工作区。
- settings 子导航完全不再出现在全局 `Sidebar` 中。

#### 验收标准
- 进入 `/settings/*` 时，看不到聊天历史与 settings 子导航的混合内容。
- 主工作区里的“设置”入口仍可正常进入设置。

#### 前置依赖
- `R-001`
- `L-001`

---

### 14.4 移动端 topbar/tabs 任务

#### 任务 ID
`M-001`

#### 目标
为 `/settings/*` 建立移动端独立壳层，包括顶部返回、页面标题、横向 tabs 和单列页面退化策略。

#### 涉及文件
- 新增：`F:\Work\AionX\src\features\settings\layout\SettingsMobileTabs.tsx`
- 新增：`F:\Work\AionX\src\features\settings\layout\SettingsTopbar.tsx`
- `F:\Work\AionX\src\features\settings\layout\SettingsLayout.tsx`
- `F:\Work\AionX\src\styles\app-shell.css`

#### 实施步骤
1. 在 layout 层判断移动端状态，切换到 `topbar + tabs + content` 结构。
2. `SettingsTopbar` 左侧固定放返回应用按钮，中间放当前页标题，右侧预留页面级操作位。
3. `SettingsMobileTabs` 读取 registry，横向滚动展示可用页面。
4. 统一 sticky 层级，保证 topbar 与 tabs 在滚动时不互相遮挡。
5. 为 split-view 页面定义移动端退化策略：
   - Model / Agent / Tools：列表优先，详情抽屉或详情页
   - WebUI：子 tab 顺序堆叠
   - Extension：host 全宽单列

#### 交付结果
- 设置页移动端壳层完成。
- 所有 settings 页面在移动端都可访问和切换。

#### 验收标准
- 移动端顶部始终可返回应用。
- tabs 可横向滚动且能准确高亮当前页面。
- 不存在双 sticky 冲突、标题遮挡或点击区域过小问题。

#### 前置依赖
- `L-001`

---

### 14.5 九个页面逐页改造任务
以下任务按推荐顺序执行。所有页面任务都必须遵守共同约束：

- 页面组件不直接 import `commands`
- 必须新增对应 `api/*.ts`
- 必须拆分出 `components/`、`hooks/`、`types.ts`
- 必须接入 registry 的 `widthPreset` 和 `layoutMode`

#### 14.5.1 Model 页面任务

##### 任务 ID
`P-001`

##### 涉及文件
- `F:\Work\AionX\src\features\settings\pages\ModelSettings.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\model\page.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\model\components\ProviderListPane.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\model\components\ProviderDetailPane.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\model\components\ProviderEditorModal.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\model\components\ModelEditorModal.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\model\hooks\useModelProviders.ts`
- 新增：`F:\Work\AionX\src\features\settings\api\model.ts`

##### 实施步骤
1. 将现有 `ModelSettings.tsx` 的 provider 列表、详情、弹窗逻辑拆分。
2. 建立 provider 选中态和详情区主从结构。
3. 替换本地 mock 健康检查为真实 API 入口。
4. 接入 typed `model` API 层。

##### 验收标准
- 桌面端双栏、移动端单列退化都成立。
- 健康检查不再使用随机延迟。

#### 14.5.2 Tools 页面任务

##### 任务 ID
`P-002`

##### 涉及文件
- `F:\Work\AionX\src\features\settings\pages\ToolsSettings.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\tools\page.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\tools\components\McpServerListPane.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\tools\components\McpServerDetailPane.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\tools\components\McpServerEditorModal.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\tools\components\ImageGenerationCard.tsx`
- 新增：`F:\Work\AionX\src\features\settings\api\tools.ts`

##### 实施步骤
1. 拆分 MCP 列表、详情、编辑弹窗。
2. 将测试连接、启停、删除、保存逻辑迁入 API 层。
3. 图像生成配置独立成单独 card。
4. 为 OAuth、只读 server、批量导入预留详情区扩展位。

##### 验收标准
- `ToolsSettings.tsx` 不再承载所有逻辑。
- MCP 管理具备主从结构。

#### 14.5.3 Gemini 页面任务

##### 任务 ID
`P-003`

##### 涉及文件
- `F:\Work\AionX\src\features\settings\pages\GeminiSettings.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\gemini\page.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\gemini\components\AccountStatusCard.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\gemini\components\ProjectBindingCard.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\gemini\hooks\useGeminiSettings.ts`
- 新增：`F:\Work\AionX\src\features\settings\api\gemini.ts`

##### 实施步骤
1. 删除 `window.prompt` 登录流程。
2. 拆分账号状态、项目绑定、网络配置、默认行为四个 section。
3. 接入 typed Gemini API。
4. 建立草稿态与服务端快照分离。

##### 验收标准
- 页面可显示真实账号状态。
- 登录与项目绑定为独立真实流程。

#### 14.5.4 WebUI 页面任务

##### 任务 ID
`P-004`

##### 涉及文件
- `F:\Work\AionX\src\features\settings\pages\WebuiSettings.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\webui\page.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\webui\components\WebuiServiceTab.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\webui\components\ChannelsTab.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\webui\components\ChannelPluginForm.tsx`
- 新增：`F:\Work\AionX\src\features\settings\api\webui.ts`

##### 实施步骤
1. 将服务与频道拆成两个内部 tab 组件。
2. 建立顶部状态、密码管理、频道管理分区。
3. 将 JSON 文本主配置模式改为表单主配置模式。
4. 接入 typed WebUI / Channel API。

##### 验收标准
- 服务与频道逻辑拆分完成。
- 顶部子 tab 结构稳定可用。

#### 14.5.5 Extension 页面任务

##### 任务 ID
`P-005`

##### 涉及文件
- `F:\Work\AionX\src\features\settings\pages\ExtensionSettings.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\extension\page.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\extension\ExtensionSettingsHost.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\extension\hooks\useExtensionTab.ts`
- 新增：`F:\Work\AionX\src\features\settings\api\extension.ts`
- 新增：`F:\Work\AionX\src\features\settings\registry\extensionRegistry.ts`

##### 实施步骤
1. 以 `tabId` 为核心读取扩展 settings tab 声明。
2. 实现 host 模式与 metadata fallback 双态页面。
3. 提供 `iframe/webview` 宿主组件。
4. 将扩展 tab 注入 registry。

##### 验收标准
- 有 host 的扩展显示真实设置页面。
- 无 host 的扩展自动回退到 metadata 页面。

#### 14.5.6 Agent 页面任务

##### 任务 ID
`P-006`

##### 涉及文件
- `F:\Work\AionX\src\features\settings\pages\AgentSettings.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\agent\page.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\agent\components\AssistantListPane.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\agent\components\AssistantDetailPane.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\agent\components\AssistantEditorModal.tsx`
- 新增：`F:\Work\AionX\src\features\settings\api\agent.ts`

##### 实施步骤
1. 将 builtin/custom 统一为主从结构。
2. 把内置配置写回和自定义 plugin 写回分离到 API 层。
3. 统一标签、状态和详情展示形式。

##### 验收标准
- 页面不再是单文件承载全部逻辑。
- 主 Agent 绑定与助手编辑流程稳定。

#### 14.5.7 System 页面任务

##### 任务 ID
`P-007`

##### 涉及文件
- `F:\Work\AionX\src\features\settings\pages\SystemSettings.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\system\page.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\system\components\RuntimeDirectoriesCard.tsx`
- 新增：`F:\Work\AionX\src\features\settings\api\system.ts`

##### 实施步骤
1. 将应用行为与目录配置拆卡。
2. 把系统信息、目录、语言切换迁入 typed API。
3. 去掉页面内部拼装 `runtimeInfo` 的逻辑。

##### 验收标准
- 系统页字段来源明确、职责清晰。

#### 14.5.8 Display 页面任务

##### 任务 ID
`P-008`

##### 涉及文件
- `F:\Work\AionX\src\features\settings\pages\DisplaySettings.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\display\page.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\display\components\ThemeCard.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\display\components\CustomCssCard.tsx`
- 新增：`F:\Work\AionX\src\features\settings\api\display.ts`

##### 实施步骤
1. 去掉与 AionUi 对齐相关文案。
2. 拆分主题、缩放、自定义 CSS 三张卡。
3. 缩放、CSS 保存迁入 typed API。

##### 验收标准
- 显示页为纯 AionX 语义。
- 自定义 CSS 有明确保存与预览策略。

#### 14.5.9 About 页面任务

##### 任务 ID
`P-009`

##### 涉及文件
- `F:\Work\AionX\src\features\settings\pages\About.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\about\page.tsx`
- 新增：`F:\Work\AionX\src\features\settings\pages\about\components\UpdateCard.tsx`
- 新增：`F:\Work\AionX\src\features\settings\api\about.ts`

##### 实施步骤
1. 清除全部 AionUi 参照文案与链接。
2. 将更新偏好、检查更新能力迁入 API 层。
3. 收敛为窄栏居中页。

##### 验收标准
- About 页完全品牌化。
- “检查更新”不再是空按钮。

---

### 14.6 建议排期顺序
建议按以下顺序排期：

1. `R-001`
2. `L-001`
3. `S-001`
4. `M-001`
5. `P-001`
6. `P-002`
7. `P-003`
8. `P-004`
9. `P-005`
10. `P-006`
11. `P-007`
12. `P-008`
13. `P-009`

执行规则：

- 未完成 `R-001` 和 `L-001`，不得开始逐页改造。
- 未完成 `S-001`，不得声称 settings 已从主工作区独立。
- 未完成 `M-001`，不得声称 settings shell 已完成。

更新说明（2026-03-17 15:18:21 +08:00）：

- 本次修订将原先混合桌面/窄屏断言的单个 smoke 用例拆分为 `desktop` 与 `responsive` 两条独立真实 Tauri WebDriver 路径，并记录了串行执行要求，原因是这样更符合桌面端 smoke 与响应式 smoke 的推荐实践，也能避免并行抢占 `4444` 端口导致的假失败。
